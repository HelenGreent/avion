// #TODO тут помилки, через видалений мною файл Compute.vue з папки components, там було наступне
// <template>
// <slot v-bind="$attrs" />
// </template>
{
  "extends": "@vue/tsconfig/tsconfig.web.json",
  "include": [
    "dts/*",
     "src/**/*", 
     "src/**/*.vue"
    ],
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "types": ["unplugin-icons/types/vue"]
  },

  "references": [
    {
      "path": "./tsconfig.config.json"
    }
  ]
}
